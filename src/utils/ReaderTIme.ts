/*
  阅读时长 = 总字数 ÷ 平均阅读速度（275 wpm） 注：wpm，全称为 Words per minute
  图片的读取时间为第一张 12 秒，第二张 11 秒，依次减少 1 秒到第 10 张之后，每张以 3 秒计算。
 */
export function getReadingTime(content: string): string {
  const type: string = ''; //xx个字  //xx分钟

  return type;
}
